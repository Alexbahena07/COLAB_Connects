{
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.3.1"
  },

  "scripts": {
    "postinstall": "prisma generate",
    "build": "prisma generate && next build",
    "digest:daily": "curl \"http://localhost:3000/api/notifications/digest?frequency=DAILY&commit=true\"",
    "digest:weekly": "curl \"http://localhost:3000/api/notifications/digest?frequency=WEEKLY&commit=true\""
  }

}
